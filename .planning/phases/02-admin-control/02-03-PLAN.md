---
phase: 02-admin-control
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified: [src/components/admin/AlojamientoForm.tsx, src/components/admin/GeocodingSearch.tsx]
autonomous: false
requirements: [ADM-01]
must_haves:
  truths:
    - "Admin can search for an address and see Lat/Lng populated automatically"
    - "Form includes switches for features like Pool, BBQ, etc."
  artifacts:
    - path: "src/components/admin/GeocodingSearch.tsx"
      provides: "Nominatim integration"
---

<objective>
Enhance the accommodation form with geocoding search and specific feature toggles.

Purpose: Simplify address input and capture detailed accommodation characteristics for future filtering.
Output: Geocoding-enabled form with boolean feature switches.
</objective>

<execution_context>
@E:/Proyectos Github/brahcaciones/.gemini/get-shit-done/workflows/execute-plan.md
@E:/Proyectos Github/brahcaciones/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-admin-control/02-CONTEXT.md
@src/components/admin/AlojamientoForm.tsx
@src/types/alojamiento.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Feature Toggles to Form</name>
  <files>src/components/admin/AlojamientoForm.tsx</files>
  <action>
    - Add shadcn `switch` component (or `checkbox`).
    - Integrate boolean fields into `AlojamientoForm`: `has_pool`, `has_bbq`, `individual_beds`, `sofa_bed`, `near_beach`.
    - Ensure these fields are sent to Supabase on Save.
  </action>
  <verify>
    <automated>grep "has_pool" src/components/admin/AlojamientoForm.tsx</automated>
  </verify>
  <done>Feature toggles added to the form.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Address Search (Geocoding)</name>
  <files>src/components/admin/GeocodingSearch.tsx</files>
  <action>
    - Create `GeocodingSearch.tsx` component.
    - Use `fetch` to query `https://nominatim.openstreetmap.org/search?format=json&q={address}`.
    - Present results in a list/dropdown.
    - When a result is selected, trigger a callback to the parent form with `{lat, lng}`.
    - Include a simple debounce or a "Buscar" button to avoid rate-limiting Nominatim.
  </action>
  <verify>
    <automated>grep "nominatim.openstreetmap.org" src/components/admin/GeocodingSearch.tsx</automated>
  </verify>
  <done>Nominatim integration component created.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Geocoding into Form & Polish</name>
  <files>src/components/admin/AlojamientoForm.tsx</files>
  <action>
    - Insert `GeocodingSearch` into the form layout.
    - Update form state (lat/lng fields) when an address is selected.
    - Add visual labels and layout improvements to the form.
    - Ensure Lat/Lng fields are still manually editable if needed (fallback).
  </action>
  <verify>
    <automated>grep "GeocodingSearch" src/components/admin/AlojamientoForm.tsx</automated>
  </verify>
  <done>Form enhanced with automatic geocoding.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Enhanced Admin Form with Geocoding and Feature Toggles.</what-built>
  <how-to-verify>
    1. Open the 'Add Accommodation' form.
    2. Toggle some features (e.g., 'Piscina', 'Barbacoa').
    3. Type an address in the search box (e.g., 'Calle Mayor, Madrid').
    4. Select a result from the list.
    5. Verify that Latitud and Longitud fields are updated automatically.
    6. Save the record and check the Supabase dashboard (or the table) to verify all fields were sent.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| App → Nominatim API | External API call for geocoding |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Spoofing | src/components/admin/GeocodingSearch.tsx | accept | Use HTTPS for Nominatim API calls |
| T-02-06 | Denial of Service | src/components/admin/GeocodingSearch.tsx | mitigate | Use debounce or button-trigger for API calls |
</threat_model>

<success_criteria>
- Geocoding automatically populates lat/lng.
- Form includes all 5 specific boolean features.
- Admin dashboard is fully functional for all Phase 2 requirements.
</success_criteria>

<output>
After completion, create `.planning/phases/02-admin-control/02-03-SUMMARY.md`
</output>
