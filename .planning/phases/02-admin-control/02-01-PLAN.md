---
phase: 02-admin-control
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [supabase/schema.sql, src/types/alojamiento.ts, src/lib/alojamientos.ts]
autonomous: true
requirements: [ADM-01]
must_haves:
  truths:
    - "The database supports the specific boolean features (pool, bbq, etc.)"
    - "The codebase has a typed interface for accommodations"
  artifacts:
    - path: "src/types/alojamiento.ts"
      provides: "Alojamiento interface"
    - path: "src/lib/alojamientos.ts"
      provides: "Supabase CRUD functions"
---

<objective>
Update the database schema and establish the data layer for accommodation management.

Purpose: Ensure the backend and types align with the Phase 2 requirements for boolean features and external URLs.
Output: SQL migration, TypeScript types, and Supabase wrapper functions.
</objective>

<execution_context>
@E:/Proyectos Github/brahcaciones/.gemini/get-shit-done/workflows/execute-plan.md
@E:/Proyectos Github/brahcaciones/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-admin-control/02-CONTEXT.md
@supabase/schema.sql
@src/lib/supabase.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Supabase Schema</name>
  <files>supabase/schema.sql</files>
  <action>
    Modify the `alojamientos` table to include the following columns:
    - `image_url` (TEXT)
    - `description` (TEXT, nullable)
    - `has_pool` (BOOLEAN, default false)
    - `has_bbq` (BOOLEAN, default false)
    - `individual_beds` (BOOLEAN, default false)
    - `sofa_bed` (BOOLEAN, default false)
    - `near_beach` (BOOLEAN, default false)
    
    Remove the generic `features` JSONB column as it's replaced by explicit boolean fields (per D-02).
  </action>
  <verify>
    <automated>grep -E "has_pool|image_url" supabase/schema.sql</automated>
  </verify>
  <done>Schema updated with specific fields.</done>
</task>

<task type="auto">
  <name>Task 2: Define Alojamiento Types</name>
  <files>src/types/alojamiento.ts</files>
  <action>
    Create a TypeScript interface `Alojamiento` that matches the database schema.
    Include `id`, `title`, `price`, `dates`, `location_lat`, `location_lng`, `link`, `image_url`, `description`, and all boolean feature flags.
  </action>
  <verify>
    <automated>test -f src/types/alojamiento.ts</automated>
  </verify>
  <done>TypeScript interface exported.</done>
</task>

<task type="auto">
  <name>Task 3: Create Supabase CRUD wrappers</name>
  <files>src/lib/alojamientos.ts</files>
  <action>
    Implement functions using the `supabase` client to:
    - `getAlojamientos()`: Fetch all records.
    - `createAlojamiento(data)`: Insert a new record.
    - `updateAlojamiento(id, data)`: Update an existing record.
    - `deleteAlojamiento(id)`: Remove a record.
    
    Ensure functions are typed with the `Alojamiento` interface.
  </action>
  <verify>
    <automated>grep "export const getAlojamientos" src/lib/alojamientos.ts</automated>
  </verify>
  <done>Data access layer ready for UI integration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client → Supabase | Untrusted data from admin form sent to DB |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Injection | supabase/schema.sql | mitigate | Use Supabase client with parameterized queries (implicit in SDK) |
| T-02-02 | Tampering | src/lib/alojamientos.ts | mitigate | In v1, we rely on pseudo-auth; ensure RLS is handled if real auth is added later |
</threat_model>

<success_criteria>
- Supabase table updated.
- TypeScript types established.
- API wrapper functions implemented and tested for presence.
</success_criteria>

<output>
After completion, create `.planning/phases/02-admin-control/02-01-SUMMARY.md`
</output>
