---
phase: 02-admin-control
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified: [src/App.tsx, src/components/admin/AdminDashboard.tsx, src/components/admin/AlojamientoForm.tsx]
autonomous: false
requirements: [ADM-01, ADM-02, ADM-03]
must_haves:
  truths:
    - "Admin can see a table of accommodations"
    - "Admin can open a form to add or edit an accommodation"
    - "Form has clear Save and Cancel buttons"
    - "Changes are persisted in Supabase"
  artifacts:
    - path: "src/components/admin/AdminDashboard.tsx"
      provides: "Main admin view"
    - path: "src/components/admin/AlojamientoForm.tsx"
      provides: "CRUD form component"
---

<objective>
Implement the admin dashboard and basic CRUD functionality for accommodations.

Purpose: Provide a user interface for managing vacation options with persistent storage.
Output: Functional Admin Dashboard with a creation/edition form.
</objective>

<execution_context>
@E:/Proyectos Github/brahcaciones/.gemini/get-shit-done/workflows/execute-plan.md
@E:/Proyectos Github/brahcaciones/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-admin-control/02-CONTEXT.md
@src/App.tsx
@src/types/alojamiento.ts
@src/lib/alojamientos.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install UI Components & Scaffold Dashboard</name>
  <files>src/components/admin/AdminDashboard.tsx</files>
  <action>
    - Add shadcn components: `table`, `dialog`, `label`, `toast`.
    - Create `AdminDashboard.tsx` that fetches accommodations from `getAlojamientos()` on mount.
    - Render a table showing: Title, Price, Dates, and "Actions" (Edit/Delete).
    - Add an "Add Accommodation" button at the top.
  </action>
  <verify>
    <automated>test -f src/components/admin/AdminDashboard.tsx</automated>
  </verify>
  <done>Admin Dashboard scaffolded with data fetching.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Alojamiento Form</name>
  <files>src/components/admin/AlojamientoForm.tsx</files>
  <action>
    Create a form component using `react-hook-form` (or simple state) with:
    - Fields: Title, Price (text), Dates (text), Link (external URL), Image URL.
    - Buttons: "Guardar" (Save) and "Cancelar" (Cancel) per D-03.
    - Integration: Handle both "Create" (POST) and "Update" (PATCH) modes.
    - Feedback: Show a toast on success/error.
  </action>
  <verify>
    <automated>grep "Guardar" src/components/admin/AlojamientoForm.tsx</automated>
  </verify>
  <done>Form component implemented with Save/Cancel buttons.</done>
</task>

<task type="auto">
  <name>Task 3: Connect Dashboard and Routing</name>
  <files>src/App.tsx</files>
  <action>
    - Modify `App.tsx` to render `AdminDashboard` when the logged-in user has the `admin` role.
    - Connect the "Delete" action in the dashboard table to `deleteAlojamiento(id)`.
    - Connect the "Edit" and "Add" buttons to open the `AlojamientoForm` inside a Dialog.
  </action>
  <verify>
    <automated>grep "AdminDashboard" src/App.tsx</automated>
  </verify>
  <done>Admin Dashboard integrated into the application shell.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Admin Dashboard with Table and CRUD Form (basic fields).</what-built>
  <how-to-verify>
    1. Login as 'admin' (using the code from .env).
    2. Click 'Add Accommodation'.
    3. Fill in basic fields (Title, Price, etc.).
    4. Click 'Guardar'.
    5. Verify the item appears in the table.
    6. Click 'Edit', change something, and Save.
    7. Click 'Delete' and confirm it disappears.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Admin UI → Supabase | CRUD operations performed by the admin role |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Elevation of Privilege | src/App.tsx | mitigate | Ensure `AdminDashboard` is ONLY rendered if `user.role === 'admin'` |
| T-02-04 | Information Disclosure | src/components/admin/AdminDashboard.tsx | accept | List is intended for internal group use |
</threat_model>

<success_criteria>
- Admin can create, edit, and delete accommodations.
- Modal form includes Save/Cancel buttons.
- UI reflects database state.
</success_criteria>

<output>
After completion, create `.planning/phases/02-admin-control/02-02-SUMMARY.md`
</output>
