---
phase: 01-foundations-access
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, vite.config.ts, tailwind.config.js, src/lib/supabase.ts, supabase/schema.sql, .env.example]
autonomous: true
requirements: [AUTH-01]

must_haves:
  truths:
    - "Project is initialized with React 19 and Tailwind 4"
    - "Supabase client is configured and ready for use"
    - "Database schema for accommodations and votes is defined"
  artifacts:
    - path: "src/lib/supabase.ts"
      provides: "Supabase client singleton"
    - path: "supabase/schema.sql"
      provides: "SQL for creating required tables"
    - path: "package.json"
      provides: "Project dependencies and scripts"
  key_links:
    - from: "src/lib/supabase.ts"
      to: "import.meta.env"
      via: "environment variables"
---

<objective>
Initialize the React + Vite project with the core tech stack (Tailwind 4, shadcn/ui) and set up the Supabase infrastructure.

Purpose: Provide the technical foundation for all subsequent features and the database persistence layer.
Output: Scaffolding, Supabase client, and initial SQL schema.
</objective>

<execution_context>
@E:/Proyectos Github/brahcaciones/.gemini/get-shit-done/workflows/execute-plan.md
@E:/Proyectos Github/brahcaciones/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project Scaffolding</name>
  <files>package.json, vite.config.ts, tailwind.config.js, src/main.tsx, src/index.css</files>
  <action>
    Initialize a Vite project with React 19 (TypeScript). 
    Install and configure:
    - Tailwind CSS 4.x (follow official v4 upgrade/init guide for Vite).
    - Basic shadcn/ui setup (init and add button/input components).
    - Essential libs: lucide-react, clsx, tailwind-merge.
    Ensure index.css imports Tailwind 4 directives.
  </action>
  <verify>
    <automated>npm run dev -- --help && ls src/index.css</automated>
  </verify>
  <done>Vite project runs, Tailwind 4 is active, and base UI components are available.</done>
</task>

<task type="auto">
  <name>Task 2: Supabase Infrastructure</name>
  <files>src/lib/supabase.ts, supabase/schema.sql, .env.example</files>
  <action>
    1. Create `supabase/schema.sql` with:
       - Table `alojamientos`: id (uuid, pk), title, price, dates, location (lat, lng), features (jsonb), link, created_at.
       - Table `votos`: id (uuid, pk), alojamiento_id (fk), usuario_slug (text), puntuacion (int), updated_at.
       - Unique constraint on `votos` for (alojamiento_id, usuario_slug).
    2. Setup `src/lib/supabase.ts` using `@supabase/supabase-js`, exporting a `supabase` client.
    3. Create `.env.example` with placeholders for `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`, and the 6 `VITE_AUTH_*` codes defined in CONTEXT.md.
  </action>
  <verify>
    <automated>ls src/lib/supabase.ts && ls supabase/schema.sql</automated>
  </verify>
  <done>Supabase client initialized and DB schema documented for manual application.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> Supabase | Data persistence and retrieval via anon-key. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | .env.example | mitigate | Ensure real keys are never committed; only placeholders in example. |
| T-01-02 | Tampering | Database | accept | Use RLS in future phases to prevent unauthorized DB changes; for Phase 1, focus is on schema. |
</threat_model>

<verification>
- Run `npm run build` to ensure no environment errors.
- Verify `supabase/schema.sql` contains the `votos` and `alojamientos` table definitions.
</verification>

<success_criteria>
- Project starts without errors.
- `supabase` client is exported from `src/lib/supabase.ts`.
- Schema file exists with the requested tables.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-access/01-01-SUMMARY.md`
</output>
