---
phase: 01-foundations-access
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified: [src/App.tsx, src/components/ProtectedRoute.tsx, src/components/Shell.tsx]
autonomous: true
requirements: [AUTH-03, AUTH-04]

must_haves:
  truths:
    - "Protected routes are inaccessible without login"
    - "App Shell shows a Logout button when authenticated"
    - "Clicking Logout redirects to Login and clears session"
  artifacts:
    - path: "src/components/ProtectedRoute.tsx"
      provides: "Wrapper to enforce authentication on routes"
    - path: "src/components/Shell.tsx"
      provides: "Main layout with Navigation and Logout"
  key_links:
    - from: "src/components/Shell.tsx"
      to: "AuthContext"
      via: "logout function"
---

<objective>
Finalize the application structure by implementing protected routing and the main App Shell with logout functionality.

Purpose: Complete the access layer and provide a consistent user interface for the protected areas of the app.
Output: ProtectedRoute component and Shell layout.
</objective>

<execution_context>
@E:/Proyectos Github/brahcaciones/.gemini/get-shit-done/workflows/execute-plan.md
@E:/Proyectos Github/brahcaciones/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundations-access/01-02-PLAN.md
@.planning/phases/01-foundations-access/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Routing & Protected Routes</name>
  <files>src/components/ProtectedRoute.tsx, src/App.tsx</files>
  <action>
    1. Create `ProtectedRoute.tsx`:
       - Checks `isAuthenticated` from `useAuth`.
       - If false, redirects to `/login`.
       - If true, renders children.
    2. Configure `react-router-dom` in `App.tsx`:
       - `/login`: Public.
       - `/admin`: Wrapped in `ProtectedRoute` (and ideally checks `isAdmin`).
       - `/dashboard`: Wrapped in `ProtectedRoute`.
    3. Add placeholder components for Admin and User dashboards.
  </action>
  <verify>
    <automated>grep -q "Redirect" src/components/ProtectedRoute.tsx || grep -q "Navigate" src/components/ProtectedRoute.tsx</automated>
  </verify>
  <done>Routes are secured and role-based access is partially enforced.</done>
</task>

<task type="auto">
  <name>Task 2: App Shell & Logout</name>
  <files>src/components/Shell.tsx, src/App.tsx</files>
  <action>
    1. Create `Shell.tsx`:
       - A layout component that wraps protected content.
       - Includes a header/sidebar with the app title.
       - Displays a "Logout" button that calls `logout()` from `useAuth`.
    2. Update `App.tsx` to use the `Shell` for authenticated routes.
  </action>
  <verify>
    <automated>grep -q "logout" src/components/Shell.tsx</automated>
  </verify>
  <done>Authenticated users see the App Shell with a functional Logout button.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> Protected UI | Routing logic blocks UI access. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Information Disclosure | Protected Routes | mitigate | Prevent sensitive UI from mounting if token/slug is missing. |
</threat_model>

<verification>
- Navigate directly to `/admin` while logged out; verify redirect to `/login`.
- Login, click Logout; verify redirect to `/login` and `localStorage` is cleared.
</verification>

<success_criteria>
- Phase 1 goal achieved: Core stack + Pseudo-Auth fully functional.
- All 4 AUTH requirements met.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-access/01-03-SUMMARY.md`
</output>
