---
phase: 03-discovery-context
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified: [src/components/ui/sheet.tsx, src/components/AccommodationSheet.tsx, src/components/DiscoveryView.tsx, src/components/AccommodationCard.tsx, src/components/MapComponent.tsx]
autonomous: true
requirements: [USER-03]

must_haves:
  truths:
    - "Clicking on an accommodation (Card or Marker) opens a Side Panel (Sheet/Drawer)"
    - "Side Panel shows all accommodation details (Price, PPP, Features, External Link)"
    - "External link opens in a new tab"
  artifacts:
    - path: "src/components/ui/sheet.tsx"
      provides: "shadcn/ui Sheet component"
    - path: "src/components/AccommodationSheet.tsx"
      provides: "Detail view for accommodation"
  key_links:
    - from: "src/components/AccommodationCard.tsx"
      to: "src/components/AccommodationSheet.tsx"
      via: "Selection callback in DiscoveryView"
    - from: "src/components/MapComponent.tsx"
      to: "src/components/AccommodationSheet.tsx"
      via: "Selection callback in DiscoveryView"
---

<objective>
Implement the side panel detail view and wire interactions from the map and list.

Purpose: Allow users to view full details of an accommodation without losing context.
Output: Working Sheet/Drawer with detailed accommodation info and external booking link.
</objective>

<execution_context>
@E:/Proyectos Github/brahcaciones/.gemini/get-shit-done/workflows/execute-plan.md
@E:/Proyectos Github/brahcaciones/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-discovery-context/03-CONTEXT.md
@.planning/phases/03-discovery-context/03-01-SUMMARY.md
@.planning/phases/03-discovery-context/03-02-SUMMARY.md
@src/components/DiscoveryView.tsx
@src/components/AccommodationCard.tsx
@src/components/MapComponent.tsx
@src/types/alojamiento.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Detail Sheet Component</name>
  <files>src/components/ui/sheet.tsx, src/components/AccommodationSheet.tsx</files>
  <action>
    - Create `src/components/ui/sheet.tsx` (standard shadcn/ui implementation).
    - Create `src/components/AccommodationSheet.tsx` which accepts an `alojamiento` and an `isOpen`/`onClose` handler.
    - Inside the sheet, display:
      - Title and location.
      - Full features list with icons.
      - Financial details: Total Price and highlighted Price Per Person (PPP).
      - Action button: "Ver en Airbnb/Booking" (opens `alojamiento.link` in new tab).
  </action>
  <verify>
    <automated>grep "SheetContent" src/components/AccommodationSheet.tsx</automated>
  </verify>
  <done>AccommodationSheet component is implemented with all required details and external link.</done>
</task>

<task type="auto">
  <name>Task 2: Wiring Interactions</name>
  <files>src/components/DiscoveryView.tsx, src/components/AccommodationCard.tsx, src/components/MapComponent.tsx</files>
  <action>
    - Add `selectedAlojamiento` state to `DiscoveryView.tsx`.
    - Pass a `onSelect` callback to `AccommodationCard` and `MapComponent`.
    - When an item is selected:
      - Update `selectedAlojamiento`.
      - If from map: Center map on the selected accommodation (optional polish).
      - Open the `AccommodationSheet`.
    - Ensure clicking a marker's popup or the marker itself triggers selection.
  </action>
  <verify>
    <automated>grep "selectedAlojamiento" src/components/DiscoveryView.tsx</automated>
  </verify>
  <done>User can click cards or markers to view details in the side panel.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Browser → Airbnb/Booking | Outbound link to external domain |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Information Disclosure | src/components/AccommodationSheet.tsx | accept | URLs are public; no PII exposure. |
</threat_model>

<verification>
Click on a card in the list. Verify the Sheet opens with the correct data. Click a marker on the map. Verify the Sheet opens. Click the external link and verify it opens in a new tab.
</verification>

<success_criteria>
Full detail viewing functionality is implemented via a side panel, integrated with both map and list views.
</success_criteria>

<output>
After completion, create `.planning/phases/03-discovery-context/03-03-SUMMARY.md`
</output>
