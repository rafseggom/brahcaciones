---
phase: 03-discovery-context
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: [package.json, src/components/MapComponent.tsx, src/components/DiscoveryView.tsx, src/index.css]
autonomous: true
requirements: [USER-02]
user_setup:
  - service: Leaflet
    why: "Map visualization"
    dashboard_config:
      - task: "No dashboard needed, uses CartoDB free tiles"

must_haves:
  truths:
    - "Interactive map displays in the left panel"
    - "Accommodations appear as markers on the map"
    - "Map theme (tiles) changes automatically when switching app mode"
  artifacts:
    - path: "src/components/MapComponent.tsx"
      provides: "Leaflet map integration"
  key_links:
    - from: "src/components/DiscoveryView.tsx"
      to: "src/components/MapComponent.tsx"
      via: "React component prop passing"
---

<objective>
Integrate Leaflet map to visualize accommodations geographically, with dynamic styling synced to the application theme.

Purpose: Provide spatial context for the accommodations.
Output: Working interactive map with markers and theme-aware tile styling.
</objective>

<execution_context>
@E:/Proyectos Github/brahcaciones/.gemini/get-shit-done/workflows/execute-plan.md
@E:/Proyectos Github/brahcaciones/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-discovery-context/03-CONTEXT.md
@.planning/phases/03-discovery-context/03-01-SUMMARY.md
@package.json
@src/components/DiscoveryView.tsx
@src/types/alojamiento.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Leaflet Setup & Dynamic Styling</name>
  <files>package.json, src/index.css, src/components/MapComponent.tsx</files>
  <action>
    - Install dependencies: `npm install leaflet react-leaflet @types/leaflet`.
    - Add Leaflet CSS import to `src/index.css`.
    - Create `src/components/MapComponent.tsx` using `MapContainer`, `TileLayer`, and `Marker` from `react-leaflet`.
    - Implement dynamic tile URL based on the current theme (using `useTheme` from `next-themes`):
      - Light: `https://{s}.basemaps.cartocdn.com/rastertiles/voyager/{z}/{x}/{y}{r}.png`
      - Dark: `https://{s}.basemaps.cartocdn.com/dark_all/{z}/{x}/{y}{r}.png`
    - Fix Leaflet default icon issues (missing images in Vite) by overriding the `Icon.Default`.
  </action>
  <verify>
    <automated>grep "react-leaflet" package.json && grep "TileLayer" src/components/MapComponent.tsx</automated>
  </verify>
  <done>MapComponent renders with correct tiles for both light and dark modes.</done>
</task>

<task type="auto">
  <name>Task 2: Map Markers & Integration</name>
  <files>src/components/MapComponent.tsx, src/components/DiscoveryView.tsx</files>
  <action>
    - Update `MapComponent` to accept `alojamientos` as a prop.
    - Render a `Marker` for each alojamiento using its `lat` and `lng`.
    - Add a simple `Popup` to the marker showing the title.
    - Implement automatic centering/fitting of the map to include all markers.
    - Replace the placeholder in `DiscoveryView` with the real `MapComponent`.
  </action>
  <verify>
    <automated>grep "Marker" src/components/MapComponent.tsx</automated>
  </verify>
  <done>Accommodations are visible as markers on the map, and the map fits the bounds of the markers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Browser → CartoDB Tiles | External network request for map assets |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Spoofing | TileLayer | accept | CartoDB tiles are public assets; low risk of impactful spoofing. |
</threat_model>

<verification>
Open the Discovery view. Confirm that markers appear for all accommodations in the database. Switch between light and dark modes and observe the map style change.
</verification>

<success_criteria>
Leaflet map is fully integrated, showing markers for all accommodations, and responds to theme changes.
</success_criteria>

<output>
After completion, create `.planning/phases/03-discovery-context/03-02-SUMMARY.md`
</output>
