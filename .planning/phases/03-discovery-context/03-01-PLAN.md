---
phase: 03-discovery-context
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/main.tsx, src/App.tsx, src/components/ui/theme-provider.tsx, src/components/ui/theme-toggle.tsx, src/components/DiscoveryView.tsx, src/components/AccommodationCard.tsx, src/components/ui/card.tsx, src/components/ui/badge.tsx]
autonomous: true
requirements: [USER-01, USER-04]

must_haves:
  truths:
    - "User can toggle between light and dark modes"
    - "Theme preference persists after page refresh"
    - "User can see a list of accommodations with Price Per Person (Total / 5)"
    - "Layout shows two distinct areas for Map (placeholder) and List"
  artifacts:
    - path: "src/components/ui/theme-provider.tsx"
      provides: "Theme management via next-themes"
    - path: "src/components/DiscoveryView.tsx"
      provides: "Split-screen layout for Discovery"
    - path: "src/components/AccommodationCard.tsx"
      provides: "Visual card for accommodation with PPP"
  key_links:
    - from: "src/App.tsx"
      to: "src/components/DiscoveryView.tsx"
      via: "Conditional rendering for user role"
---

<objective>
Setup the foundation for the Discovery phase, including theme management and the split-screen layout shell.

Purpose: Provide users with a visual entry point and the ability to toggle dark mode.
Output: Working theme toggle, Discovery layout, and accommodation list with PPP calculations.
</objective>

<execution_context>
@E:/Proyectos Github/brahcaciones/.gemini/get-shit-done/workflows/execute-plan.md
@E:/Proyectos Github/brahcaciones/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-discovery-context/03-CONTEXT.md
@package.json
@src/App.tsx
@src/lib/alojamientos.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Theme Management & Toggle</name>
  <files>src/components/ui/theme-provider.tsx, src/components/ui/theme-toggle.tsx, src/main.tsx, src/App.tsx</files>
  <action>
    - Wrap the application in `ThemeProvider` from `next-themes` in `src/main.tsx`.
    - Create `src/components/ui/theme-toggle.tsx` using a button that switches between 'light' and 'dark'.
    - Update the App header in `src/App.tsx` to include the `ThemeToggle`.
    - Ensure Tailwind 4 dark mode classes (`dark:`) work as expected.
  </action>
  <verify>
    <automated>grep -r "ThemeProvider" src/main.tsx && grep -r "ThemeToggle" src/App.tsx</automated>
  </verify>
  <done>User can switch themes and the preference persists in localStorage.</done>
</task>

<task type="auto">
  <name>Task 2: Discovery Layout & UI Components</name>
  <files>src/components/ui/card.tsx, src/components/ui/badge.tsx, src/components/DiscoveryView.tsx</files>
  <action>
    - Create basic `Card` and `Badge` components in `src/components/ui/` (standard shadcn/ui style).
    - Create `src/components/DiscoveryView.tsx` with a split-screen layout:
      - Left panel (60% desktop): Map placeholder (a themed div with "Map goes here").
      - Right panel (40% desktop): Scrollable list of accommodations.
    - Fetch alojamientos using `getAlojamientos()` from `src/lib/alojamientos.ts`.
  </action>
  <verify>
    <automated>ls src/components/DiscoveryView.tsx</automated>
  </verify>
  <done>DiscoveryView renders with a map area and a list area.</done>
</task>

<task type="auto">
  <name>Task 3: Accommodation Cards & PPP</name>
  <files>src/components/AccommodationCard.tsx, src/components/DiscoveryView.tsx</files>
  <action>
    - Create `src/components/AccommodationCard.tsx` to display:
      - Title, location, and dates.
      - Total price.
      - **Price Per Person (PPP)** calculated as `price / 5`. Use a bold highlight for PPP.
      - Feature icons (Lucide React) for key attributes.
    - Update `DiscoveryView` to render the list using `AccommodationCard`.
    - Handle empty states if no accommodations are found.
  </action>
  <verify>
    <automated>grep "/ 5" src/components/AccommodationCard.tsx</automated>
  </verify>
  <done>List shows cards with correct PPP calculation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Browser → Supabase | Fetching accommodations based on pseudo-auth session |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | src/lib/alojamientos.ts | mitigate | Ensure RLS in Supabase allows only authenticated users to read. |
| T-03-02 | Tampering | LocalStorage | accept | Theme preference is low-risk; no security impact if tampered. |
</threat_model>

<verification>
Verify that logging in as a user displays the DiscoveryView instead of the placeholder. Test theme switching.
</verification>

<success_criteria>
User can see the split view with accommodation cards and toggle dark mode.
</success_criteria>

<output>
After completion, create `.planning/phases/03-discovery-context/03-01-SUMMARY.md`
</output>
