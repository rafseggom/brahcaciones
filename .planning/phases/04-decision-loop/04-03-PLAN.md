---
phase: 04-decision-loop
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified: [src/components/DiscoveryView.tsx, src/lib/alojamientos.ts]
autonomous: true
requirements: [VOTE-02]

must_haves:
  truths:
    - "Accommodations are sorted by average rating once the user has voted for something"
    - "Users can manually refresh the list to see updated votes"
  artifacts:
    - path: "src/components/DiscoveryView.tsx"
      provides: "Sorting and refresh logic"
  key_links:
    - from: "DiscoveryView"
      to: "avg_rating"
      via: "array.sort comparison"
---

<objective>
Implement the automatic ranking logic and finalize the synchronization strategy.

Purpose: Surface the most popular options to facilitate group decision-making.
Output: Updated DiscoveryView with participation-based sorting.
</objective>

<execution_context>
@E:/Proyectos Github/brahcaciones/.gemini/get-shit-done/workflows/execute-plan.md
@E:/Proyectos Github/brahcaciones/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-decision-loop/04-CONTEXT.md
@.planning/phases/04-decision-loop/04-02-SUMMARY.md
@src/components/DiscoveryView.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update getAlojamientos to include vote aggregates</name>
  <files>src/lib/alojamientos.ts</files>
  <action>
    Modify `getAlojamientos` (or create a new specialized function) to fetch accommodations joined with vote aggregates from Supabase:
    - Use a Postgres View or a complex `.select()` (e.g., `.select('*, votos(puntuacion)')`) to calculate `avg_rating`, `total_votes`, and the current user's vote.
    - Note: In Phase 4 Context D-04, simplicity is preferred. If a join is too complex, fetch votes separately and merge in memory.
  </action>
  <verify>
    <automated>Verify the returned Alojamiento objects contain the new vote fields.</automated>
  </verify>
  <done>Data fetching includes vote metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Implement participation-based sorting</name>
  <files>src/components/DiscoveryView.tsx</files>
  <action>
    In `DiscoveryView`:
    - Check if the current user has participated in voting (at least one `user_vote !== undefined`).
    - Implement a `useMemo` for the sorted accommodations:
      - Default: Sort by `created_at` (insertion order).
      - Participating: Sort by `avg_rating` (descending), then by `total_votes` (descending).
    - Ensure the list re-renders when a vote is submitted (D-03).
  </action>
  <verify>
    <automated>Check for sorting logic implementation using avg_rating.</automated>
  </verify>
  <done>Automatic ranking is active for participating users.</done>
</task>

<task type="auto">
  <name>Task 3: Add manual refresh trigger</name>
  <files>src/components/DiscoveryView.tsx</files>
  <action>
    Add a refresh button (Refresh icon from `lucide-react`) in the header or toolbar of the `DiscoveryView`:
    - Trigger a re-fetch of the accommodations and votes when clicked.
    - This satisfies D-04 (simple manual sync).
  </action>
  <verify>
    <automated>Check for a button that calls the fetch function.</automated>
  </verify>
  <done>Manual refresh is available to the user.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Data Integrity | Sorting logic relies on the data provided by the backend (Supabase). |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Denial of Service | DiscoveryView | accept | Manual refresh is user-initiated; unlikely to cause DoS in a 6-user app. |
</threat_model>

<verification>
Ensure ranking works correctly and refresh updates the view.
</verification>

<success_criteria>
- Accommodations sort correctly by average rating.
- Ranking only activates after the user's first vote.
- Refresh button works.
</success_criteria>

<output>
After completion, create `.planning/phases/04-decision-loop/04-03-SUMMARY.md`
</output>
