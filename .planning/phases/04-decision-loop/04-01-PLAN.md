---
phase: 04-decision-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [supabase/schema.sql, src/lib/votos.ts, src/types/alojamiento.ts, src/lib/alojamientos.ts]
autonomous: true
requirements: [VOTE-01, VOTE-03]

must_haves:
  truths:
    - "Votes can be saved and updated in the database associated with a user slug"
    - "Alojamiento types support vote metadata"
  artifacts:
    - path: "supabase/schema.sql"
      provides: "votos table definition"
    - path: "src/lib/votos.ts"
      provides: "CRUD for votes (upsert)"
    - path: "src/types/alojamiento.ts"
      provides: "Updated Alojamiento interface"
  key_links:
    - from: "votos"
      to: "alojamientos"
      via: "alojamiento_id foreign key"
---

<objective>
Establish the database schema and data access layer for the voting system.

Purpose: Provide a reliable way to store, update, and retrieve user votes.
Output: Supabase table, TypeScript types, and data fetching functions.
</objective>

<execution_context>
@E:/Proyectos Github/brahcaciones/.gemini/get-shit-done/workflows/execute-plan.md
@E:/Proyectos Github/brahcaciones/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-decision-loop/04-CONTEXT.md
@supabase/schema.sql
@src/types/alojamiento.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and apply votos table schema</name>
  <files>supabase/schema.sql</files>
  <action>
    Ensure the `votos` table is defined in `supabase/schema.sql` per D-01:
    - `id` UUID PRIMARY KEY
    - `alojamiento_id` UUID REFERENCES `alojamientos(id)`
    - `usuario_slug` TEXT
    - `puntuacion` INT (0-5)
    - `UNIQUE(alojamiento_id, usuario_slug)` for upsert support.
    Apply the schema to the Supabase project if not already present.
  </action>
  <verify>
    <automated>Check if table exists in Supabase dashboard or via CLI if available</automated>
  </verify>
  <done>Table `votos` exists in the database with the specified constraints.</done>
</task>

<task type="auto">
  <name>Task 2: Update Alojamiento type with vote metadata</name>
  <files>src/types/alojamiento.ts</files>
  <action>
    Extend the `Alojamiento` interface to include optional fields for the voting system:
    - `avg_rating?: number`
    - `total_votes?: number`
    - `user_vote?: number` (the vote of the currently logged-in user)
  </action>
  <verify>
    <automated>grep -E "avg_rating|total_votes|user_vote" src/types/alojamiento.ts</automated>
  </verify>
  <done>Interface updated and exported.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement voting data layer</name>
  <files>src/lib/votos.ts</files>
  <behavior>
    - `upsertVote(alojamientoId, usuarioSlug, puntuacion)`: Should insert or update the vote.
    - `getVotesSummary(alojamientoId)`: Should return average and count.
  </behavior>
  <action>
    Create `src/lib/votos.ts` using `@supabase/supabase-js`:
    - Implement `upsertVote` using `.upsert()` on the `votos` table.
    - Implement a function to fetch vote aggregates (average and count) per alojamiento.
  </action>
  <verify>
    <automated>Verify functions are exported and handle Supabase responses correctly.</automated>
  </verify>
  <done>Functions implemented and verified with basic error handling.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client -> Supabase | User votes are sent directly to Supabase via the client SDK. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | votos table | mitigate | Supabase RLS (Row Level Security) should eventually restrict users to only update their own votes (usuario_slug match). |
</threat_model>

<verification>
Check that the `votos` table is ready and the library functions can communicate with it.
</verification>

<success_criteria>
- Supabase has the `votos` table.
- `lib/votos.ts` provides `upsertVote`.
- `Alojamiento` type is updated.
</success_criteria>

<output>
After completion, create `.planning/phases/04-decision-loop/04-01-SUMMARY.md`
</output>
