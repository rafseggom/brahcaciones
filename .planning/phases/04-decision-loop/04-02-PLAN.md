---
phase: 04-decision-loop
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified: [src/components/ui/StarRating.tsx, src/components/AccommodationCard.tsx, src/components/AccommodationSheet.tsx]
autonomous: false
requirements: [VOTE-01, VOTE-02]

must_haves:
  truths:
    - "Users can click stars to vote"
    - "Average ratings are hidden if the user hasn't voted for that property"
  artifacts:
    - path: "src/components/ui/StarRating.tsx"
      provides: "Star selection and display component"
    - path: "src/components/AccommodationCard.tsx"
      provides: "Updated card with voting support"
  key_links:
    - from: "StarRating"
      to: "upsertVote"
      via: "onChange callback"
---

<objective>
Implement the voting UI components and the "Blind Voting" logic to prevent bias.

Purpose: Allow users to rate accommodations while ensuring they aren't influenced by others' votes initially.
Output: Reusable StarRating component and updated accommodation views.
</objective>

<execution_context>
@E:/Proyectos Github/brahcaciones/.gemini/get-shit-done/workflows/execute-plan.md
@E:/Proyectos Github/brahcaciones/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-decision-loop/04-CONTEXT.md
@.planning/phases/04-decision-loop/04-01-SUMMARY.md
@src/components/AccommodationCard.tsx
@src/components/AccommodationSheet.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create StarRating component</name>
  <files>src/components/ui/StarRating.tsx</files>
  <action>
    Create a `StarRating` component using `lucide-react`'s `Star` icon:
    - Props: `rating` (current), `onChange` (optional), `readOnly` (boolean), `showText` (boolean).
    - It should render 5 stars.
    - If not `readOnly`, stars should be clickable and hoverable to select a value 1-5 (0 is also possible if clicking the same value).
    - Handle color for active (gold/yellow) vs inactive (gray).
  </action>
  <verify>
    <automated>Check for file existence and exported component.</automated>
  </verify>
  <done>Component `StarRating` is functional and reusable.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate voting into AccommodationCard and Sheet</name>
  <files>src/components/AccommodationCard.tsx, src/components/AccommodationSheet.tsx</files>
  <action>
    - Add the `StarRating` component to both views.
    - Use `useAuth` to get the current `usuario_slug`.
    - In `AccommodationCard` (list view), show the user's current vote if it exists.
    - Implement the "Blind Voting" logic (D-02):
      - If `user_vote` is undefined/null, hide `avg_rating` and `total_votes`.
      - Display a placeholder like "Vota para ver la media" or a locked icon.
      - Once the user votes (via `upsertVote`), reveal the stats.
  </action>
  <verify>
    <automated>Check for useAuth and StarRating imports in both files.</automated>
  </verify>
  <done>UI reflects the blind voting logic and allows submitting votes.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Verify Voting Interaction</name>
  <what-built>Voting interface with blind logic.</what-built>
  <how-to-verify>
    1. Log in as a user.
    2. Go to the discovery view.
    3. Verify that for an unvoted accommodation, the average score is hidden.
    4. Click a star to vote.
    5. Verify the vote is sent (check network tab if possible) and the average score/total votes are revealed.
    6. Verify you can change your vote.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| UI Logic | Visibility of data is controlled by client-side logic based on user_vote presence. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Information Disclosure | AccommodationCard | accept | While technically the data might be in the JSON response, the UI hiding it is sufficient for preventing "casual" bias in this non-adversarial internal app. |
</threat_model>

<verification>
Check that voting works and the blind logic is respected in the UI.
</verification>

<success_criteria>
- StarRating component works.
- Average scores are hidden until user votes.
- Voting updates the local state and Supabase.
</success_criteria>

<output>
After completion, create `.planning/phases/04-decision-loop/04-02-SUMMARY.md`
</output>
