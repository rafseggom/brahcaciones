---
phase: 05-refinement-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [supabase/schema.sql, src/types/alojamiento.ts, src/lib/alojamientos.ts, src/components/admin/AlojamientoForm.tsx, src/components/admin/AdminDashboard.tsx]
autonomous: true
requirements: [DATA-01, DATA-02]
must_haves:
  truths:
    - "The 'dates' field is removed from the database and UI"
    - "A 'rooms' field is added to the database and Admin form"
  artifacts:
    - path: "supabase/schema.sql"
      provides: "Updated database schema"
    - path: "src/types/alojamiento.ts"
      provides: "Updated Alojamiento interface"
    - path: "src/lib/alojamientos.ts"
      provides: "Updated service logic for rooms"
  key_links:
    - from: "src/components/admin/AlojamientoForm.tsx"
      to: "supabase"
      via: "updateAlojamiento/createAlojamiento"
      pattern: "alojamiento\\.rooms"
---

<objective>
Update the data model to focus on relevant accommodation metrics by removing the 'dates' field and adding a 'rooms' count.

Purpose: Simplify the data structure and prepare for room-based information display.
Output: Updated database schema, types, and administrative CRUD logic.
</objective>

<execution_context>
@E:/Proyectos Github/brahcaciones/.gemini/get-shit-done/workflows/execute-plan.md
@E:/Proyectos Github/brahcaciones/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-refinement-polish/05-CONTEXT.md
@supabase/schema.sql
@src/types/alojamiento.ts
@src/lib/alojamientos.ts
@src/components/admin/AlojamientoForm.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Supabase Schema</name>
  <files>supabase/schema.sql</files>
  <action>
    Modify the `alojamientos` table in `supabase/schema.sql`:
    - Remove the `dates` column.
    - Add a `rooms` column of type `INTEGER` with a default of 0.
    
    Then, execute the migration in the Supabase SQL Editor to reflect changes in the live DB.
  </action>
  <verify>
    <automated>Verify column existence in Supabase Dashboard.</automated>
  </verify>
  <done>Database schema updated and applied.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update Types and Service Logic</name>
  <files>src/types/alojamiento.ts, src/lib/alojamientos.ts</files>
  <behavior>
    - `Alojamiento` interface should NOT have `dates`.
    - `Alojamiento` interface SHOULD have `rooms: number`.
    - `createAlojamiento` and `updateAlojamiento` should handle the `rooms` field.
  </behavior>
  <action>
    Update `src/types/alojamiento.ts` to remove `dates` and add `rooms: number`.
    Update `src/lib/alojamientos.ts` to ensure all CRUD operations include the `rooms` field (though current spread/partial might already handle it, explicitly check).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Types and services synchronized with the new schema.</done>
</task>

<task type="auto">
  <name>Task 3: Update Admin Forms and Dashboard</name>
  <files>src/components/admin/AlojamientoForm.tsx, src/components/admin/AdminDashboard.tsx</files>
  <action>
    Refactor `AlojamientoForm.tsx`:
    - Remove the "Fechas" input field.
    - Add a "Habitaciones" input field (type number).
    - Update `formData` state and `useEffect` to handle `rooms` instead of `dates`.
    
    Refactor `AdminDashboard.tsx`:
    - Remove the "Fechas" column from the Table.
    - Add a "Hab" or "Habitaciones" column showing the room count.
  </action>
  <verify>
    <automated>Run app and verify Admin panel reflects changes.</automated>
  </verify>
  <done>Admin UI reflects the simplified data model.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client -> Supabase | Input validation for the new `rooms` field |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Injection | supabase/schema.sql | mitigate | Use server-side validation/types to ensure `rooms` is an integer |
</threat_model>

<verification>
Verify that creating and editing an accommodation works without the dates field and correctly saves the number of rooms.
</verification>

<success_criteria>
1. `alojamientos` table has `rooms` and no `dates`.
2. Admin Dashboard displays `rooms` and can edit it.
3. No build errors from type mismatches.
</success_criteria>

<output>
After completion, create `.planning/phases/05-refinement-polish/05-01-SUMMARY.md`
</output>
