---
phase: 05-refinement-polish
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified: [src/App.tsx, src/components/admin/AdminDashboard.tsx, src/components/ui/dialog.tsx, src/components/ui/sheet.tsx, src/components/ui/theme-toggle.tsx]
autonomous: true
requirements: [REF-01, REF-02, REF-04]
must_haves:
  truths:
    - "The application occupies the full viewport width (no max-width containers)"
    - "Dark mode persists across page refreshes and affects all UI components"
    - "Modals and sheets have opaque backgrounds and heavy backdrops"
  artifacts:
    - path: "src/components/admin/AdminDashboard.tsx"
      provides: "Full-width layout for admin"
    - path: "src/components/ui/dialog.tsx"
      provides: "Opaque modal styles"
  key_links:
    - from: "src/main.tsx"
      to: "ThemeProvider"
      via: "storageKey"
      pattern: "storageKey=\"vite-ui-theme\""
---

<objective>
Overhaul the application layout to use the full screen width and ensure the theme engine correctly handles manual toggling and persistence.

Purpose: Improve visual space utilization and UX consistency.
Output: Full-width layout components, fixed theme persistence, and refined modal visibility.
</objective>

<execution_context>
@E:/Proyectos Github/brahcaciones/.gemini/get-shit-done/workflows/execute-plan.md
@E:/Proyectos Github/brahcaciones/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-refinement-polish/05-CONTEXT.md
@src/App.tsx
@src/components/admin/AdminDashboard.tsx
@src/components/ui/dialog.tsx
@src/components/ui/sheet.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-width Layout Overhaul</name>
  <files>src/App.tsx, src/components/admin/AdminDashboard.tsx, src/components/DiscoveryView.tsx</files>
  <action>
    - In `src/components/admin/AdminDashboard.tsx`, remove the `container mx-auto` classes from the main wrapper to allow the dashboard to occupy the full width.
    - Check `src/App.tsx` and `src/components/DiscoveryView.tsx` for any `max-w-*` or `container` classes that might be restricting the width and remove them.
    - Ensure padding is consistent (`px-6` or `px-8`) across all full-width views.
  </action>
  <verify>
    <automated>Visual check that content touches side margins only with specified padding.</automated>
  </verify>
  <done>Layout is full-width across Admin and User views.</done>
</task>

<task type="auto">
  <name>Task 2: Fix Manual Theme Persistence</name>
  <files>src/components/ui/theme-toggle.tsx, src/components/ui/theme-provider.tsx</files>
  <action>
    - Ensure `next-themes` is correctly configured to use `class` strategy in `ThemeProvider`.
    - Verify that `ThemeToggle` correctly updates the theme and that `ThemeProvider` in `main.tsx` uses a stable `storageKey`.
    - Fix any flickering issues by ensuring the `bg-white dark:bg-zinc-950` transitions are smooth.
  </action>
  <verify>
    <automated>Refresh page and check if theme preference remains.</automated>
  </verify>
  <done>Theme preference is manual and persistent.</done>
</task>

<task type="auto">
  <name>Task 3: Opaque Modals and Heavy Backdrops</name>
  <files>src/components/ui/dialog.tsx, src/components/ui/sheet.tsx</files>
  <action>
    - Update `DialogContent` and `SheetContent` styling:
      - Change backgrounds to be fully opaque (e.g., `bg-white dark:bg-zinc-950` instead of semi-transparent).
      - Increase the opacity of the `DialogOverlay` and `SheetOverlay` (e.g., `bg-black/80` or `backdrop-blur-md`).
    - Ensure that the background content is effectively obscured to focus the user on the modal content.
  </action>
  <verify>
    <automated>Open a dialog and verify the background is heavily obscured and modal is opaque.</automated>
  </verify>
  <done>Modals are visually distinct and focused.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | Information Disclosure | Layout | accept | Full-width layout doesn't expose sensitive data |
</threat_model>

<verification>
Verify full-width behavior in both Admin and User views. Test theme toggle persistence across multiple page refreshes.
</verification>

<success_criteria>
1. App occupies 100% viewport width.
2. Theme persists after F5.
3. Modals are opaque and have visible backdrops.
</success_criteria>

<output>
After completion, create `.planning/phases/05-refinement-polish/05-02-SUMMARY.md`
</output>
