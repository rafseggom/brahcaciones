---
phase: 05-refinement-polish
plan: 03
type: execute
wave: 3
depends_on: [05-01, 05-02]
files_modified: [src/index.css, src/components/admin/AlojamientoForm.tsx, src/components/AccommodationCard.tsx, src/components/AccommodationSheet.tsx, src/components/DiscoveryView.tsx]
autonomous: true
requirements: [REF-03, USER-05, USER-06]
must_haves:
  truths:
    - "App uses warm colors and gradients (orange/pink/yellow) in highlights and cards"
    - "Admin features use Toggle Buttons instead of switches/checkboxes"
    - "Total Price is the most prominent price information on accommodation cards"
    - "Room count is visible on the cards"
  artifacts:
    - path: "src/index.css"
      provides: "Option B aesthetic (colors, shadows, gradients)"
    - path: "src/components/admin/AlojamientoForm.tsx"
      provides: "Feature Toggle Buttons UI"
    - path: "src/components/AccommodationCard.tsx"
      provides: "Refined card with Total Price focus and room count"
  key_links:
    - from: "src/components/AccommodationCard.tsx"
      to: "alojamiento.rooms"
      via: "display"
      pattern: "alojamiento\\.rooms"
---

<objective>
Implement the 'Playful/Travel' aesthetic and refine the user interface for better data visibility and interaction quality.

Purpose: Finalize the visual identity and improve the core discovery experience.
Output: Themed application with enhanced cards and interactive admin controls.
</objective>

<execution_context>
@E:/Proyectos Github/brahcaciones/.gemini/get-shit-done/workflows/execute-plan.md
@E:/Proyectos Github/brahcaciones/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-refinement-polish/05-CONTEXT.md
@src/index.css
@src/components/admin/AlojamientoForm.tsx
@src/components/AccommodationCard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply Playful/Travel Aesthetic (Option B)</name>
  <files>src/index.css, src/App.css</files>
  <action>
    - Update `src/index.css` variables:
      - Change `--accent` to a warm orange-to-pink gradient friendly color (e.g., `#f97316`).
      - Define a new `--gradient-primary` using `linear-gradient(to right, #f97316, #ec4899)`.
      - Soften `--shadow` with a larger blur and lower opacity.
      - Update `--border` to be lighter and softer.
    - Add Tailwind utility classes or global CSS for:
      - `.bg-gradient-playful`: applying the primary gradient.
      - `.shadow-soft`: custom shadow implementation.
    - Update base styles for Buttons and Cards to have slightly more border-radius (e.g., `rounded-xl`).
  </action>
  <verify>
    <automated>Visual check for warm colors and gradients in the UI.</automated>
  </verify>
  <done>Option B aesthetic applied globally.</done>
</task>

<task type="auto">
  <name>Task 2: Admin Feature Toggle Buttons</name>
  <files>src/components/admin/AlojamientoForm.tsx</files>
  <action>
    - Install the Toggle component if missing: `npx shadcn@latest add toggle`.
    - Refactor the "Características" section in `AlojamientoForm.tsx`:
      - Replace `Switch` components with `Toggle` buttons.
      - Each feature (Pool, BBQ, etc.) should be a large button with its icon and label.
      - Style active states with the new playful gradient or a solid accent color.
      - Ensure the layout is a grid (e.g., `grid-cols-2` or `grid-cols-3`) for these buttons.
  </action>
  <verify>
    <automated>Verify features can be toggled and visual feedback is clear.</automated>
  </verify>
  <done>Admin UI uses Toggle Buttons for features.</done>
</task>

<task type="auto">
  <name>Task 3: Refine Discovery Cards and Sheet</name>
  <files>src/components/AccommodationCard.tsx, src/components/AccommodationSheet.tsx, src/components/DiscoveryView.tsx</files>
  <action>
    - In `AccommodationCard.tsx`:
      - Move the "Total Price" to the top right or make it significantly larger than PPP.
      - Display PPP as a secondary, smaller label (e.g., "60€/persona").
      - Add a `Bed` or `Home` icon followed by the `rooms` count next to the features list.
      - Apply a hover scale effect and a subtle gradient border on hover.
    - In `AccommodationSheet.tsx`:
      - Update the header and info sections to match the new aesthetic (warm colors, icons).
    - In `DiscoveryView.tsx`:
      - Add a `LayoutGroup` or simple CSS transitions for card entrance and list updates to make it feel "alive".
  </action>
  <verify>
    <automated>Open DiscoveryView and verify card layout and animations.</automated>
  </verify>
  <done>User UI is polished and information hierarchy is optimized.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Tampering | Admin UI | accept | Aesthetic changes don't affect authorization |
</threat_model>

<verification>
Verify the full end-to-end flow: Admin creates accommodation with Toggle Buttons -> User sees refined card with prominent Total Price and Room count -> Transitions are smooth.
</verification>

<success_criteria>
1. Application reflects Option B colors and gradients.
2. Admin feature selection uses Toggle Buttons.
3. User cards show Total Price as primary and include rooms count.
4. Micro-interactions are present.
</success_criteria>

<output>
After completion, create `.planning/phases/05-refinement-polish/05-03-SUMMARY.md`
</output>
